import { Helmet } from "react-helmet";

function Settings() {
    return (
        <div>
            <Helmet>
                <title>설정 | 틴에이저코드</title>
            </Helmet>
        </div>
    );
}

export default Settings;
