import { Helmet } from "react-helmet";

function Logout() {
    return (
        <div>
            <Helmet>
                <title>로그아웃 | 틴에이저코드</title>
            </Helmet>
        </div>
    );
}

export default Logout;