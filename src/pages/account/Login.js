import { Helmet } from "react-helmet";

function Login() {
    return (
        <div>
            <Helmet>
                <title>로그인 | 틴에이저코드</title>
            </Helmet>
        </div>
    );
}

export default Login;
